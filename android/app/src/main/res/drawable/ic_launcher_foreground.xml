<?xml version="1.0" encoding="utf-8"?>
<!--
    Empty foreground layer — the new app icon (icon.png at the repo root) is a
    single full-bleed illustration used as the adaptive icon's background
    layer (see drawable-*dpi/ic_launcher_background.png), so no separate
    foreground artwork is needed.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108" />
